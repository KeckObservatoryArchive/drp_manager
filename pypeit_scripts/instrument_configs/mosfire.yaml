user_cfg : {
    rdx: {
        ignore_bad_headers : 'True',
    },
    calibrations : {
        flatfield : {
            saturated_slits : 'mask'
        }
    }
}

fits_table_thresholds : {
}

allowed_keywords : {
    lampstat01 : 'off,on' # Don't use arcs (Ne or Ar)
}
